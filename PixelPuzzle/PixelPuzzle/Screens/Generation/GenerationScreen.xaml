<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="PixelPuzzle.Screens.Generation.GenerationScreen"
    xmlns:controls="clr-namespace:PixelPuzzle.Controls"
    NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="{Binding AdBannerHeight}" />
            </Grid.RowDefinitions>

            <Image
                Source="bg_tutorial.png"
                Grid.RowSpan="8"
                Aspect="AspectFill" />

            <Grid
                Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <BoxView
                    Grid.ColumnSpan="9"
                    BackgroundColor="White"
                    Opacity=".9"/>

                <ImageButton
                    Grid.Column="1"
                    Padding="10"
                    WidthRequest="{Binding ToolbarButtonSize}"
                    HeightRequest="{Binding ToolbarButtonSize}"
                    Source="back.png"
                    Clicked="Back_Clicked"
                    CornerRadius="5" />

                <Button
                    Grid.Column="3"
                    Padding="10"
                    WidthRequest="150"
                    HeightRequest="{Binding ToolbarButtonSize}"
                    Text="Share"
                    Clicked="Share_Clicked"
                    CornerRadius="5" />
            </Grid>

            <Grid
                Grid.Row="3">
                <controls:PuzzleControl
                    x:Name="PuzzleControl"
                    BindingContext="{Binding PuzzleControlViewModel}"/>
            </Grid>

            <controls:CellStateSelection
                Grid.Row="5"
                BindingContext="{Binding PuzzleControlViewModel}" />

            <controls:AdMobView
                Grid.Row="7"
                VerticalOptions="End" />
        </Grid>
    </ContentPage.Content>
</ContentPage>