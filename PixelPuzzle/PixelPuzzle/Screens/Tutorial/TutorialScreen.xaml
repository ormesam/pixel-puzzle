<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="PixelPuzzle.Screens.Tutorial.TutorialScreen"
    xmlns:controls="clr-namespace:PixelPuzzle.Controls"
    NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button
                Grid.Row="1"
                Text="{Binding CurrentStep.Message}"
                Clicked="MoveNext_Clicked"/>

            <Grid
                Grid.Row="2"
                InputTransparent="{Binding IsTutorialComplete, Converter={StaticResource InvertBoolConverter}}">
                <controls:PuzzleControl
                    x:Name="PuzzleControl"
                    BindingContext="{Binding PuzzleControlViewModel}"/>
            </Grid>

            <Grid
                Grid.RowSpan="4"
                IsVisible="{Binding PuzzleControlViewModel.IsComplete}">
                <Grid.RowDefinitions>
                    <RowDefinition
                        Height="*"/>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="Auto"/>
                    <RowDefinition
                        Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="*" />
                    <ColumnDefinition
                        Width="Auto" />
                    <ColumnDefinition
                        Width="*" />
                </Grid.ColumnDefinitions>

                <BoxView
                    Grid.RowSpan="4"
                    Grid.ColumnSpan="3"
                    BackgroundColor="White"
                    Opacity=".75"/>

                <BoxView
                    Grid.Row="1"
                    Grid.RowSpan="2"
                    Grid.Column="1"
                    CornerRadius="25"
                    BackgroundColor="White" />

                <Label
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="25,25,25,0"
                    FontSize="Large"
                    Text="Tutorial Complete!" />
                <Button
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="25"
                    Text="Let me at them!"
                    BackgroundColor="Green"
                    TextColor="White"
                    Clicked="Complete_Clicked"
                    CornerRadius="50"/>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>