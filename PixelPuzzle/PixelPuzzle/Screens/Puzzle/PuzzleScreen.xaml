<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:userControls="clr-namespace:PixelPuzzle.Screens.Puzzle.UserControls"
    x:Class="PixelPuzzle.Screens.Puzzle.PuzzleScreen">
    <ContentPage.Content>
        <Grid>
            <userControls:GamePlaying
                x:Name="GamePlaying"
                IsVisible="{Binding IsPlaying}"/>

            <userControls:GameNotPlaying
                IsVisible="{Binding IsNotPlaying}" />

            <userControls:GameComplete
                IsVisible="{Binding IsComplete}" />
        </Grid>
    </ContentPage.Content>
</ContentPage>